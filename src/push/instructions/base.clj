(ns push.instructions.base
  (:use [push.instructions.core])
  (:use [push.instructions.dsl])
  (:use [push.instructions.base.boolean])
  (:use [push.instructions.base.conversion])
  (:use [push.instructions.base.integer]))
