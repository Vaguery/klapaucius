(ns push.types.standard.plush-genome
  (:require [push.instructions.core :as core])
  (:require [push.types.core :as t])
  (:require [push.instructions.dsl :as d])
  )

;; TODO


; autoconstructive_boolean_rand
; autoconstructive_integer_rand
; genome_close_dec
; genome_close_inc
; genome_gene_copy
; genome_gene_copy_range
; genome_gene_delete
; genome_gene_dup
; genome_gene_randomize
; genome_new
; genome_parent1
; genome_parent2
; genome_rotate
; genome_silence
; genome_toggle_silent
; genome_unsilence



; (def plush-genome-type
;   ( ->  (t/make-type  :plush-genome
;                       :recognizer ;; no idea
;                       :attributes #{:genome})
;         t/make-visible 
;         t/make-equatable 
;         t/make-movable
;         ))

